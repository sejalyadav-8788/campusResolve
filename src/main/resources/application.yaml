spring:
  application:
    name: campusresolve-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/campusresolve
    username: postgres
    password: sejal@#1516

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080